<div class="container text-center">
    <h1> Most Popular Tweets </h1>


    <% url = URI("https://api.twitter.com/2/tweets/1424540000000000000?tweet.fields=attachments,author_id,created_at,entities,geo,id,in_reply_to_user_id,lang,possibly_sensitive,referenced_tweets,source,text,withheld") %>

    <% https = Net::HTTP.new(url.host, url.port) %>
    <% https.use_ssl = true %>

    <% request = Net::HTTP::Get.new(url) %>

    <%response = https.request(request) %>
    <%= puts response.read_body %>

    

        
</div>

